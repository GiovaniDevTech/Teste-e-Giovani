
let nome = "";
let sobreNome = "";
let turma = "";

let nota1 = 4.9;
let nota2 = 5.2;
let nota3 = 7.5;
let nota4 = 2.7;

let media = "";
let professor = "";

// Atribuindo valor as variáveis

nome = "Giovani";
sobreNome = "Vieira Lavall";
turma = "T1";
professor = "Gabriel";

media = (nota1 + nota2 + nota3 + nota4) / 4; // Aqui é somente um soma

console.log(`A média do aluno ${nome} é ${media}`);
//console.log(`A média do aluno ${nome} é ${(nota1 + nota2 + nota3 + nota4) / 4}`);

let turmaTernario = false
let resultadoTurma = turmaTernario ? "A" : "B"; // Isso daqui é igual
let resultadoTurma2 = true ? "A" : "B"; // Isso daqui é igual

console.log(`O nome do aluno(a) ${nome} ${sobreNome}, está matriculado(a) na turma ${resultadoTurma}.`);

let hora = 21;

let digaOla = hora < 19 ? "Bom dia!" : (hora > 18 ? "Boa tarde!" : "Bom noite!");

let digaOlaEverson = hora < 19 ? `Bom dia!` : (hora > 18 ? `Boa noite!` : `Boa tarde!`);

console.log("Obrigado Enisno me aprender. Professor: " +  professor )

console.log(digaOlaEverson)

// Verificação de maior idade
// Operador Unário
// Binário 
// Verdadeiro ou falso
// Ternário
// Operador E
// Operador OU

// Posto de gasoline
let tanque = 40; // 40 Litros
let tanqueAtual = 5; // 5 Litros
let alcool = false;
let gasolina = true;
let modelo = "Ford";

